package org.collperf



import collection._



/* statistics */
object statistics {

  def ANOVA(history: Seq[Seq[Long]], newest: Seq[Long]): Boolean = {
    false
  }

  def CI(alt1: Seq[Long], alt2: Seq[Long]): Boolean = {
    false
  }

  def CoV(measurements: Seq[Long]): Boolean = {
    false
  }

}