name: Release snapshot

on:
  push:
    tags:
      - '*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Test
        run: sbt +test
      - name: Get tag
        id: tag
        uses: dawidd6/action-get-tag@v1
        with:
          # Optionally strip `v` prefix
          strip_v: true
      - name: Publish release
        run: SONATYPE_USER=com.github.hughsimpson SONATYPE_PASS="${SOME_STORED_SECRET}" sbt -Dversion="${{steps.tag.outputs.tag}}" +publish